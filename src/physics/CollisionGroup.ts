export enum CollisionGroup {
  A = Math.pow(2, 0),
  B = Math.pow(2, 1),
  C = Math.pow(2, 2),
  D = Math.pow(2, 3),
  E = Math.pow(2, 4),
  F = Math.pow(2, 5),
  G = Math.pow(2, 6),
  H = Math.pow(2, 7),
  I = Math.pow(2, 8),
  J = Math.pow(2, 9),
  K = Math.pow(2, 10),
}
